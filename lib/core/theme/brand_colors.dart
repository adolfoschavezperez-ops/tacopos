import 'package:flutter/material.dart';

class BrandColors {
  const BrandColors._();

  static const backgroundPrimary = Color(0xFF090909);
  static const backgroundSecondary = Color(0xFF111111);
  static const surfaceDark = Color(0xFF171717);
  static const surfaceSoft = Color(0x0FFFFFFF);

  static const textPrimary = Color(0xFFF5F5F5);
  static const textSecondary = Color(0xFFCFCFCF);
  static const textMuted = Color(0xFF9A9A9A);

  static const accentYellow = Color(0xFFFFD54A);
  static const accentOrange = Color(0xFFF59A23);
  static const accentOrangeDeep = Color(0xFFD96E18);
  static const accentGlow = Color(0x2EFFD54A);

  static const glassFill = Color(0x14FFFFFF);
  static const glassBorder = Color(0x29FFFFFF);
  static const glassHighlight = Color(0x2EFFFFFF);

  static const success = Color(0xFF55D98B);
  static const danger = Color(0xFFFF6B6B);
  static const info = Color(0xFF7AB8FF);

  static const black = backgroundPrimary;
  static const surface = surfaceDark;
  static const surfaceHigh = Color(0xFF202020);
  static const yellow = accentYellow;
  static const orange = accentOrange;
  static const orangeDark = accentOrangeDeep;
  static const white = textPrimary;
  static const muted = textMuted;
}
